apiVersion: v1
kind: Service
metadata: 
       name: clusterip
spec:
    selector:
        app: mypod
    type: ClusterIp
    ports:
      - protocol: TCP
        port: 80
        name: nginx
